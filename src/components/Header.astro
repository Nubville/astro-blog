---

---

<header slot="header">
  <a href="/" class="brand" aria-label="Andrew's Dev Blog — home"> Andrew's Dev Blog </a>
</header>

<style>
  header {
    align-items: center;
    display: flex;
    padding: 1rem 2rem;
  }

  .brand {
    color: var(--brand-1);
    font-family: var(--font-display);
    font-size: var(--font-size-3);
    font-weight: var(--font-weight-light);
    letter-spacing: 0.05em;
    text-decoration: none;
    white-space: nowrap;

    &:hover {
      color: var(--brand-3);
    }
    &:focus-visible {
      border-radius: 2px;
      outline: 2px solid var(--gold-1);
      outline-offset: 4px;
    }
  }
</style>
