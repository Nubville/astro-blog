---
interface SidebarPost {
  title: string;
  pubDate: string | Date;
  url: string;
}

interface Props {
  posts: SidebarPost[];
  postsLabel?: string;
}

const { posts, postsLabel = '// more posts' } = Astro.props;
---

{
  posts.length > 0 && (
    <nav class="sidebar-posts" aria-label="More posts">
      <p class="sidebar-posts-label">{postsLabel}</p>
      <ul role="list" class="sidebar-posts-list">
        {posts.map((post) => (
          <li class="sidebar-post-item">
            <a href={post.url} class="sidebar-post-link">
              {post.title}
            </a>
            <span class="sidebar-post-date">
              {new Date(post.pubDate).toISOString().slice(0, 10)}
            </span>
          </li>
        ))}
      </ul>
    </nav>
  )
}

<style>
  /* No outer margin — the Stack that places this owns the space around
     it. See src/components/Stack.astro. */

  .sidebar-posts-label {
    color: var(--ink-faint);
    font-family: var(--font-mono);
    font-size: var(--ag-font-size-xs);
    letter-spacing: 0.08em;
    margin: 0 0 var(--ag-space-xs);
  }

  .sidebar-posts-list {
    display: flex;
    flex-direction: column;
    gap: var(--ag-space-xs);
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .sidebar-post-item {
    border: 1px solid var(--pencil);
    border-radius: var(--radius-cel);
    padding: var(--ag-space-xs) var(--ag-space-s);
  }

  /* Dark themes only — see the matching comment in global.css on
     sketch-card for why. */
  :global([data-theme='lamplight']) .sidebar-post-item,
  :global([data-theme='horde']) .sidebar-post-item {
    background: var(--surface-raised);
  }

  .sidebar-post-link {
    color: var(--ink);
    display: block;
    font-family: var(--font-display);
    font-size: var(--ag-font-size-m);
    line-height: 1.3;
    margin-bottom: var(--ag-space-2xs);
    text-decoration: none;

    &:hover {
      color: var(--brand-1);
    }

    &:focus-visible {
      border-radius: 2px;
      outline: 2px solid var(--brand-1);
      outline-offset: 3px;
    }
  }

  .sidebar-post-date {
    color: var(--ink-faint);
    display: block;
    font-family: var(--font-mono);
    font-size: var(--ag-font-size-xs);
    letter-spacing: 0.08em;
  }
</style>
